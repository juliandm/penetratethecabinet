apiRevision: edurata.io/v1
runtime: python3_8
interface:
  inputs:
    properties:
      file:
        type: file
      cursor:
        type: file
        title: start from
        description: if to start from a specific element in the array
  outputs:
    properties:
      file:
        type: file
      cursor:
        type: string
        title: cursor
        description: if to start from a specific element in the array
        default: 0
